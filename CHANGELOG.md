# Changelog

## 0.2.1

- Hardened model roster filtering by using explicit `data-model`/`data-capability` attributes on cards.
- Fixed ESLint npm script for flat-config compatibility so `npm run lint` executes correctly.
- Added runtime interaction tests for roster search/filter behavior.
- Added shared script-block parsing helper to support future multi-script layouts.

## 0.2.0

- Added interactive model roster filtering (search + capability cycling) with live result feedback.
- Extended UI validation tests to cover the new roster controls.

## 0.1.0

- Added the initial AiPowerHouse UI shell and validation tooling.
